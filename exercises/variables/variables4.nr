// variables4.nr
// use the `hint` watch subcommand for a hint.

// I AM NOT DONE

fn main() {
    let x = 3;
    let str1 = f"x is {x}";
    println(str1);
    x = 5; // don't change this line
    println(f"x is now {x}");
}
